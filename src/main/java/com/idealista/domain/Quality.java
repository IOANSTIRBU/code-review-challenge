package com.idealista.domain;

/**
 * Clase que indica las dos calidad que debe tener el anuncio
 * es decir en calidad de alta definicio (HD) o (SD) en calidad normal
 */
public enum Quality {
    HD,
    SD,
}
